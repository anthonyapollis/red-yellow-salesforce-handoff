Read START_HERE_CLAUDE.md first. The user explicitly requires intentionally dirty data. The default import plan uses data/dirty_loadable. Preserve intended semantic defects; raw structural failures in data/dirty_raw are a separate quarantine exercise, not importable records.
