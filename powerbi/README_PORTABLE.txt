Close Power BI without saving the old report. On every computer, after extracting the entire ZIP, right-click Setup-DataPaths.ps1 and choose Run with PowerShell. Then open RedAndYellow.pbip and click Refresh. Rerun setup whenever you move the folder. File.Contents requires absolute paths; setup resolves them from the extracted folder. All 23 parquet files must stay in data.